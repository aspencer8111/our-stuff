= form_for(@item, multipart: true) do |f|
  = f.label :name
  = f.text_field :name
  = f.label :description
  = f.text_area :description
  = f.label :picture
  = f.file_field :picture
  = f.submit "Submit"
