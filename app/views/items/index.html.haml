%p.lead Feel free to claim any item you may want/need
.row
  - @items.each do |item|
    .col-sm-4.col-lg-4.col-md-4
      .thumbnail
        - unless item.picture_updated_at.nil?
          = image_tag item.picture.url(:square)
        .caption
          %h4= item.name
          %p= item.description
          - if current_user
            - if current_user.admin
              = link_to "Edit Item", edit_item_path(item), class: 'btn btn-primary'
            -else
              = form_for(item) do |f|
                = f.hidden_field :user_id
                = f.submit "Claim item"
          - else
            %p= link_to "Sign in to claim", new_user_session_path
