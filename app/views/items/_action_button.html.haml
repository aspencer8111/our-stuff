- if current_user
  - if current_user.admin
    = link_to "Edit Item", edit_item_path(item), class: 'btn btn-default btn-sm pull-right'
  -else
    = form_for(item) do |f|
      = f.hidden_field :user_id
      = f.submit "Claim item", class: 'btn btn-default btn-sm'
